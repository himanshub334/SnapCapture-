import { CameraView, useCameraPermissions } from 'expo-camera';

/*
 * JS-facing camera service.
 *
 * The project also contains reference native implementations under ios/ and
 * android/ showing how the same API can be bridged directly to AVFoundation
 * and CameraX in a bare React Native application.
 */

export { CameraView, useCameraPermissions };
